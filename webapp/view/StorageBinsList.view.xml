<mvc:View xmlns:mvc="sap.ui.core.mvc" xmlns="sap.m" xmlns:l="sap.ui.layout" xmlns:core="sap.ui.core"
          controllerName="glw.controller.StorageBinsList"
          displayBlock="true">
    <Page title="{i18n>appTitle}" showNavButton="true" navButtonPress=".onNavBack"
          class="sapUiResponsiveContentPadding">
        <Table
                id="storageBinTable"
				noDataText="Keine Lagerplätze vorhanden"
                mode="Delete"
                delete=".onDeleteStorageBinPress"
                items="{path: 'storageBins>/rows', sorter: {path: 'value/id'}}">
            <headerToolbar>
                <Toolbar>
                    <Title text="Lagerplätze ({storageBins>/total_rows})" level="H2" width="9rem"/>
                    <ToolbarSpacer/>
					<SearchField liveChange=".onSearchStorageBin" search=".onSearchStorageBin"/>
                    <Button icon="sap-icon://add" press=".onOpenAddStorageBinDialogPress"/>
                </Toolbar>
            </headerToolbar>
            <columns>
                <Column
                        width="12em">
                    <Text text="Lagerplatznummer"/>
                </Column>

            </columns>
            <items>
                <ColumnListItem>
                    <cells>
                        <Text text="{storageBins>value/id}"/>
                    </cells>
                </ColumnListItem>

            </items>
        </Table>

    </Page>
</mvc:View>
