<mvc:View xmlns:mvc="sap.ui.core.mvc" xmlns="sap.m" xmlns:l="sap.ui.layout" xmlns:core="sap.ui.core"
          controllerName="glw.controller.BatchList"
          displayBlock="true">
    <Page title="{i18n>appTitle}" showNavButton="true" navButtonPress=".onNavBack"
          class="sapUiResponsiveContentPadding">
        <Table
                id="batchTable"
				noDataText="Keine Charge vorhanden"
                mode="Delete"
                delete=".onDeleteBatchPress"
                items="{path: 'batches>/rows', sorter: {path: 'value/id'}}">
            <headerToolbar>
                <Toolbar>
                    <Title text="Chargen ({batches>/total_rows})" level="H2" width="9rem"/>
                    <ToolbarSpacer/>
					<SearchField liveChange=".onSearchBatch" search=".onSearchBatch"/>
                    <Button icon="sap-icon://add" press=".onOpenAddBatchDialogPress"/>
                </Toolbar>
            </headerToolbar>
            <columns>
                <Column
                        width="12em">
                    <Text text="Produkt"/>
                </Column>
                <Column
                        minScreenWidth="Tablet"
                        demandPopin="true">
                    <Text text="Chargendatum"/>
                </Column>

            </columns>
            <items>
                <ColumnListItem>
                    <cells>
                        <Text text="{parts: ['batches>value/productCategory', 'productCategories>/rows'], formatter: '.formatter.formatProductCategory'}"/>
                        <Text text="{path: 'batches>value/batchDate'}"/>
                    </cells>
                </ColumnListItem>

            </items>
        </Table>

    </Page>
</mvc:View>
